# CLAUDE.md

이 앱에서 작업하는 Claude 를 위한 지침. **여기 적힌 것은 이미 정해진 것만이다.**
아직 안 정한 것은 "**미정**"으로 두었다 — 채우려면 사람이 먼저 결정한다. 지어내지 않는다.

- 전체 명세: `DESIGN.md`  ·  이식 순서: `README.md`
- 회차별 판단 기록: `my-report/판단기준.md`
- 도메인 명세 원본(5행 표)·실측 근거: `../mydomain/나의_성장퍼널.md` · `../mydomain/notes/`

---

## 이 앱이 무엇인가

FDA GRAS 등록 심사가 어느 단계에서 얼마나 걸러지는지 보고, 등록을 준비하는 실무 조직에게
제출 전 점검 우선순위를 제안하는 분석·자동리포트 앱. 사람은 게이트 세 곳에서만 판단한다.

---

## 그레인 — 무엇을 하나로 세는가

**1건 = 과제번호 1개.** 고유하게 센다(`nunique`), 행을 세지 않는다.

| 데이터 | 과제번호 컬럼 |
|---|---|
| 합성 이벤트 로그 `funnel_events_synthetic` | `case_id` |
| 실제 `GRASNotices` | `grn` |

- 완전중복 행이 섞여 있어(합성 176행) 행으로 세면 첫 단계가 부풀고 전환율이 낮게 나온다.
- 재제출 묶음(`chain`) 그레인으로도 계산 가능 — `funnel(fe, id_col="chain")`.
- **미정**: 기본 보고 그레인을 건(`grn`)으로 할지 묶음(`chain`)으로 할지.
  실측 차이 5.02%p — `../mydomain/notes/퍼널실측_2026-08-29.md`

---

## 퍼널 단계와 조건

`config.FUNNEL_STEPS = ["제출", "종결", "결과_성공"]` — 순서가 곧 단계 순서다.

| 단계 | 실제 `GRASNotices` 조건 | 합성 데이터 조건 |
|---|---|---|
| 제출 | 인벤토리에 GRN 1건이 존재 | `stage == "제출"` 이벤트가 있는 건 |
| 종결 | `FDA's Letter` != `Pending` (= `Date of closure` 있음) | `stage == "종결"` |
| 결과_성공 | `FDA's Letter` == `no questions` (이의 없이 종결) | `stage == "결과_성공"` |

- `결과_실패` 는 **단계가 아니라 결과 분기**다. `FUNNEL_STEPS` 에 넣지 않는다 —
  넣으면 종결 뒤 두 갈래가 생겨 "앞 단계를 반드시 거치는가"가 깨진다.
- 순서 위반 0건으로 확인됨 (결과 ⊆ 종결 ⊆ 제출). 진짜 퍼널이다.
- `metrics.funnel()` 은 각 단계를 **독립해서 센다** — 앞 단계 통과를 강제하지 않는다.
  순서가 깨지면 표에 드러나야 한다.
- **미정**: 실제 `GRASNotices`(건당 1행 스냅샷)를 이벤트 로그 모양으로 바꾸는 어댑터.
  현재 앱 화면은 합성 이벤트 로그를 그린다.
- ⚠ **어댑터가 없다 ≠ 실측 퍼널을 못 센다.** 세 단계 전부 GRN 1행짜리 속성이라
  스냅샷에서 바로 세어진다 — 제출 = 전체 GRN 1,336 · 종결 = `cat != "Pending"` 1,301 ·
  결과_성공 = `cat == "no_questions"` 1,056 (병목 81.17%). 어댑터가 막는 것은
  **`funnel()`·`funnel_by()` 함수에 실측을 먹이는 것**뿐이다. → 아래 「지표 §정해진 것」

---

## 지표

### 정해진 것 — 최종 성공률 (재제출 체인 기준)

"이 물질이 결국 등록되는가."  정의서: `../mydomain/notes/03_지표정의.md`

```
분자 = 해소된 체인 중 no_questions 멤버가 하나라도 있는 체인 수
분모 = 해소된 체인 수 (체인 안에 Pending 멤버가 없음)
지표값 = 분자 / 분모
```

- 체인 = `Resubmission`/`Resubmitted` 링크를 union-find 로 묶은 같은 물질의 재제출 묶음
- 현재값 **86.19%** (N=1,224 · K=1,055 · 95% CI [84.15, 88.01])
- **미정**: `metrics.kpis()` 코드 반영 (아직 미구현)

### 가드레일 — 지표는 정해짐 · 임계값 미정

정의서: `../mydomain/notes/03_지표정의.md`

| 가드레일 | 계산 | 현재값 | 방향 |
|---|---|---|---|
| ① 종결까지 소요 기간 | `Date of closure` - `Date of filing` | 평균 195.6일 · 중앙 176일 · P90 290일 (**N=676**, 1998~2019만) | 높을수록 나쁨 |
| ② 재제출 필요 비율 | 다건 체인 수 / 전체 체인 수 | 5.80% (73 / 1,259) | 높을수록 나쁨 |

- **N=676 이다(678 아님).** 접수일이 있는 678건에서 GRN 813·824(접수일==종결일, 0일 심사
  아티팩트)를 코드가 뺀 값 — `metrics._grn_frame()`. 중앙값 176은 빼기 전과 같지만 평균·N은 다르다.
- 접수일이 사실상 2016년까지만 있어 ①로 최근 건은 감시할 수 없다.
  ⚠ "2017년 이후 없음"이 아니다 — **5건 있다** (2017:2 · 2018:1 · 2019:2). 2020년 이후가 0건이다.
  결론(감시 불가)은 그대로지만 "없다"는 사실이 아니므로 그렇게 쓰지 않는다.

### 유지 퍼널 — 정의도 구현도 됨  (2026-09-05 갱신: 전에 "구현 미정"이었다)

명세 5행: **첫 제출 후 1,095일(3년) 안에 다른 물질로 재제출이 없으면 이탈** (Notifier=회사 기준)

- 코호트 N=688 · 잔존 138 (20.06%) · 이탈 550 (**79.94%**)
- 관측 기간이 다른 대상을 그냥 비교하지 않는다 — 코호트 + 고정창으로 본다
  (코호트 없이 단순 비율은 26.53% 로 오히려 높게 나온다)
- `config.RETENTION_STEPS` = 통지자 → 다회 제출 → 재방문도 성공. `metrics.retention_funnel()` 구현됨
- ⚠ **앱 구현에는 코호트 필터가 없다.** 오래된 회사가 유리한 생존 편향이 남아
  화면 값(832 → 223 → 191곳)과 위 코호트 값(20.06%)이 다르다 → 리포트 7장 한계에 적혀 있다
- 정의서: `../mydomain/notes/04_유지이탈.md`

### 정해진 것  (2026-09-05 갱신: 전에 "미정"이었다)

- `kpis()` 카드 넷 = 신규 과제 수 · 최종 승인율 · 평균 처리 일수 · 반려율. 전부 GRN 그레인
- ⚠ **반려 ≠ "무사 종결도 미종결도 아닌 것".** `cat` 에 `MIXED` 가 1건 있는데
  `no_questions`·`Pending`·`no_basis`/`ceased` 어디에도 안 들어간다. 그래서 갈래 합계가
  전체와 1건 어긋난다 — 승인 1,056 + 반려 244 + Pending 35 = **1,335** / 전체 **1,336**.
  값은 0.07%p라 안 흔들리지만, 검산할 때 이 1건을 찾느라 헤매지 않도록 적어 둔다
- 분해 축 `metrics.funnel_by()` **구현됨**(Day3). 축은 `bio`(숙주식별/미분류)·`country`(미국/해외)

#### 분해 축의 실측값은 있다 — 함수만 못 돌린다  (2026-09-08 정정)

전에 여기 *"실측에는 못 돌린다. 분해는 합성 데이터로만"* 이라고 적혀 있었다. **틀렸다.**
정확히는 **함수**를 못 돌리는 것이지 **값**이 없는 것이 아니다. 이 문장 때문에 근거로 쓸 수
있는 실측 발견을 버릴 뻔했다.

| | 실측으로 되나 | 왜 |
|---|---|---|
| `funnel_by()` **함수** 호출 | **안 된다** | 이벤트 로그를 요구하는데 어댑터가 없다 |
| 그 **값** 자체 | **된다** | 종결·결과·`bio`·`country` 가 전부 GRN 1행짜리 속성이라 스냅샷에서 세어진다 |

실측값은 이미 측정돼 있다 — `../mydomain/notes/퍼널실측_2026-08-29.md` §4·§5.
구간 종결 → 결과_성공, 비중 분모 = 종결 도달 **1,301건**:

| 축 `bio` | 도달 | 전환 | 전환율 | 비중 |
|---|---:|---:|---:|---:|
| 숙주식별 | 343 | 313 | **91.25%** | 26.36% |
| 미분류 | 958 | 743 | **77.56%** | 73.64% |

격차 **13.69%p** (95% CI 겹치지 않음). 국가·종결연도로 층화해도 유지된다(§5) — 연도의 대리물이 아니다.
축 `country` 는 격차 **1.50%p** 로 `MIN_GAP`(5%p) 미달 → **"이 축으로는 안 갈린다"**.
이것도 결과이므로 값을 적는다(감춘 것이 아니다).

**그래서 이렇게 쓴다** (2026-09-08 결정):

- **화면은 합성 그대로 둔다.** 코드도 안 고친다 — 화면 격차는 13.48%p 로 나온다
- **발견·리포트의 축 분해는 실측값을 인용한다.** 출처를 문장에 박는다 —
  `실측 GRASNotices (종결 도달 n=1,301) · notes/퍼널실측_2026-08-29 §4`
- ⚠ **"앱에서 조회했다"고 쓰지 않는다.** 앱 함수가 낸 값이 아니다. 대신 재현 경로를 적는다:
  그레인 GRN 1건(nunique) · 종결 `cat != "Pending"` · 전환 `cat == "no_questions"` · 축 `bio`
- ⚠ **화면(13.48%p)과 리포트(13.69%p)가 다르다**는 사실을 7장 한계에 남긴다.
  판단기준.md 2026-09-03 의 "화면 숫자 ≠ 결론 숫자"에 붙는 항목이 하나 늘었다

---

## 임계값과 근거

`core/config.py` 에 있는 것:

`THRESHOLDS` 키 이름 = `kpis()` 가 돌려주는 지표 이름과 같아야 색이 붙는다.
"높을수록 나쁨" 지표는 `metrics.status_of()` 의 `higher_is_worse` 집합에도 이름이 있어야 한다.
정하는 순서: 방향 있는 지표만 → 현재값 조회 → 정상 변동 폭(CI/연도범위) → 경고=그 바깥 → 위험=더 아래(근거).

| 지표 | 방향 | 경고 | 위험 | 근거 |
|---|---|---:|---:|---|
| 최종 승인율 | 낮을수록 나쁨 | 75 | 70 | 경고 = 95% CI 하단(76.8%) 아래. 위험 = 최근 10년 최저(71.2%)도 안 간 선. notes/03 의 체인 기준 80/75 를 GRN 단위(분모=전체 접수 1,336)로 옮긴 값 |
| 반려율 | 높을수록 나쁨 | 22 | 30 | 경고 = 95% CI 상단(20.4%) 위. 위험 = 최근 10년 최고(29%)를 넘는 전례 없는 수준 |
| 평균 처리 일수 | 높을수록 나쁨 | 210 | 300 | 경고 = 연도별 median 최댓값(2008년 208일) 위. 위험 = 전체 P90(290일) 위 |

- 현재값은 셋 다 `ok` (승인율 79.0% · 반려율 18.3% · 처리일수 176일).
- ⚠ **평균 처리 일수는 사실상 감시 불가.** 접수일이 2016년까지만 사실상 완전하고
  2017~2019는 5건뿐이라 `monthly()` 의 이 열은 **2019년부터 NaN** 이다 → 리포트 7장 한계로.
- 가드레일 ①(종결까지 소요 기간) = 위 "평균 처리 일수"와 같은 지표. ②(재제출 필요 비율)는 임계값 **미정**.

| 항목 | 값 | 근거 |
|---|---|---|
| `MIN_SAMPLE` | 30 | 교안 기본값. 월평균 표본 4건이라 월 단위로는 거의 못 채움 — 분기·연도 단위에서만 실효 |
| `PERIOD` | 1998-05-27 ~ 2026-07-06 | 실측 `GRASNotices` 의 유효구간 (종결일 기준) |

---

## 검증 규칙 (`core/validate.py` `run_checks()`)

| 규칙 | 차단 | 경고 |
|---|---|---|
| 행 수 | 0행 또는 `MIN_SAMPLE` 미만 | 기대 행수(`EXPECTED_ROWS`)와 다름 |
| 필수 컬럼 | `REQUIRED_COLS` 중 하나라도 없음 | — |
| 날짜 범위 | 대상의 기대 범위(`DATE_SPEC` span) 벗어남 | span 안이지만 `PERIOD` 만 넘음 (합성의 설계상 정상) |

근거는 `my-report/판단기준.md` 의 2026-09-01 항목에.

---

## 분해 축 판정 — 못 믿으면 계산하지 않는다  (Day3, 2026-09-03)

`metrics.funnel_by()` 가 분해 칸마다 **판정을 계산보다 먼저** 하고, `metrics.trust_check()`
로 못 믿을 칸을 가려낸다. 판정 배지는 `metrics.funnel_by_verdict()`.
정의서: `my-report/판단기준.md` 2026-09-03 항목.

### 못 믿을 조건 셋 (`trust_check`) — 하나라도 걸리면 사유 문자열, 다 통과하면 `None`

| 조건 | 기준값 | 출처 |
|---|---|---|
| ① 표본이 모자란다 | 칸의 `도달` < **150** | `config.MIN_CELL` |
| ② 기간이 안 찼다 | 미결 ÷ (도달 + 미결) > **5%** | `config.MAX_PENDING_RATIO` |
| ③ 비교가 공정하지 않다 | 칸이 잔여 범주(`(미매칭)`·`미상`) · 또는 호출자가 `unfair` 사유를 넘김 | `metrics._RESIDUAL_LABELS` |

- ②의 "미결" = step_to 도달 실패를 '진행 중'으로 볼 때만(제출→종결 구간, `missing_is_pending=True`).
  종결→결과 구간은 미결 0이라 ②가 안 걸린다.
- `MIN_SAMPLE`(30, **테이블 행** 하한)과 `MIN_CELL`(150, **표 한 칸** 하한)은 다른 개념이다.

### 판정 순서 — 사람이 아니라 코드가 지킨다

```
1. 못 믿을 조건 (trust_check)   → 걸리면 그 칸은 사유만. 여기서 끝. 지표 계산 안 함
2. 주지표가 움직였는가          → 남은 칸 간 전환율 격차 < config.MIN_GAP (5%p)
                                 이면 "차이 없음 (이 축으로는 안 갈린다)"
3. 가드레일                     → 분해 격차 비교엔 가드레일이 없다.
                                 카드에 "가드레일 없음 — 무엇을 희생했는지 확인되지 않음"
4. 격차 >= 5%p 이고 남은 칸 >= 2 → "주목할 만함"
```

좋은 결과를 먼저 보면 뒤 확인을 대충 한다. 그래서 2를 통과했다고 바로 성공으로 가지 않는다.
`p값`·`신뢰구간`·`"통계적으로 유의"` 는 쓰지 않는다 (실험이 없다).

### 못 믿을 때 화면에 무엇을 내보내나

- **계산 자체를 하지 않는다.** `funnel_by()` 는 못 믿을 칸의 전환율·비중을 `NaN` 으로 둔다 —
  계산해 놓고 숨기는 것이 아니라 꺼낼 값이 없다. (교안 2-2)
- **감춘 칸에는 사유만 적는다.** 전환율·비중·증감 어떤 수치도 적지 않는다.
- 사유에는 **실제 숫자**를 넣는다 — "표본 부족"(X) → "표본 133건 (최소 150건)"(O).
- 색은 판정 배지에만: 주목할 만함 `warn` · 차이 없음 `none` · 감춘 칸 `block`.
  제목·테두리·차트 축에 판정 색을 쓰지 않는다.

### 미정

- `MIN_CELL` · `MAX_PENDING_RATIO` · `MIN_GAP` 값은 현재 데이터의 칸 크기 기준으로 정했다.
  데이터가 바뀌면 다시 잡는다.
- ③의 `unfair` 를 실제로 넘기는 전후·구간 비교 카드는 아직 없다 (실습 C 확장 몫).

---

## 리포트 8장 — 누가 쓰는가  (Day4, 2026-09-05)

`report/sections.py` 가 조립한다. 가르는 질문은 하나다 —
**이 문장이 틀렸을 때 누가 책임지는가.**

| 장 | 누가 | 무엇 |
|---|---|---|
| 1 요약 · 3 방법 · 4 결과 · 5 실험 · 7 한계 | 자동 | 값 · 계산 방법 · 한계 |
| **2 배경 · 6 해석 · 8 제안** | **사람** | 왜 했는가 · 무엇을 뜻하는가 · 무엇을 할 것인가 |

- 사람이 쓰는 장을 **자동으로 채우지 않는다.** 비면 PDF에 `[작성되지 않음]` 이 찍힌다
- 3 방법에는 **그레인을 반드시 적는다.** 읽는 사람이 다시 세어볼 수 있어야 한다
- 4 결과에 "왜"를 넣지 않는다 — 그건 6 해석이고 사람이 쓴다
- 5 실험: 이 도메인엔 무작위 배정이 없다. 제출 시점별 묶음 비교로 대신하되
  **"인과를 주장할 수 없다"를 본문 첫 문단에** 둔다 (각주 아님)

### 인과 단정 표현 검사 — `check_phrasing()`

`BANNED` 에 걸리면 화면에 경고가 뜬다. **자동 장뿐 아니라 사람이 쓴 장에도 건다** —
사람이 더 자주 쓴다.

```
때문에 · 덕분에 · 효과로 · 입증되었 · 증명되었 · 확실히
인해 · 기인한다 · 초래했다 · 야기했다 · 달성했다     ← 이 도메인에서 더한 것
```

### 7장 한계는 조립한다 — 사람이 매번 쓰지 않는다

**검증에서 경고가 났는데 한계에 없으면 그 경고는 없던 일이 된다.** 세 곳에서 모은다.

| 출처 | 무엇 |
|---|---|
| 검증 경고 | `validate.run_checks()` 의 `level == "warn"` 전부 |
| 못 한 것 | `trust_check` 에 걸려 판정 안 한 칸 · 임계값이 미정인 가드레일 |
| 찾았는데 없던 것 | **"없음"도 결과다** — 안 적으면 다음 사람이 또 찾는다 |

두 문장은 **항상** 넣는다: 관측 데이터라 인과 주장 불가 · `PERIOD` 보다 긴 주기는 관측 안 됨.

### 문서로 내보낼 때

- **폰트에 없는 글자를 쓰지 않는다.** `−`(U+2212) · `≤` · `②` 는 Noto Sans KR 에 없어
  PDF에서 **경고 없이 사라진다.** 각각 `-` · "이하" · `2` 로 쓴다
- 4장 차트는 합성 데이터라 **바로 위 문단에 그 사실을 적는다.** 차트만 잘라내도 오해가 없게

### 미정

- 게이트 **판단 근거를 비운 채로 통과**시킬 수 있게 둘 것인가.
  현재는 막지 않는다 — 2026-09-05 실행에서 게이트 1이 빈 근거로 통과된 기록이 남았다

---

## 제안서 — `report/proposal.py`  (Day2, 2026-09-09)

리포트와 **다른 문서**다. `report/sections.py` 를 지우거나 고치지 않고, 같은 원칙을
따로 된 파일에 적용한다. 카드(`my-report/제안카드.md`)를 절로 조립한다.

### 자동/사람 분리 — `sections.py` 와 같은 원칙

가르는 질문도 같다 — **이 절의 내용이 데이터를 다시 조회하면 똑같이 나오는가.**

| 절 | 누가 | 왜 |
|---|---|---|
| 한 장 요약 · 하지 말 것 · 다시 할 것 · 할 것 · 부록(근거 상세) | 자동 | 카드에서 옮긴 값 |
| **이 제안이 틀린다면 · 적용** | **사람** | 판단이 들어간다 |

- **판단이 들어가는 절을 자동화하면 책임의 주체가 사라진다.** 리포트 6·8장과 같은 이유다
- `build()` 는 `sections.build()` 와 **같은 형태**를 돌려준다 — `{"title", "kind", "body", ...}` 리스트
- 화면은 `kind` 하나로만 분기한다. **자동 절에는 입력창을 두지 않는다** — 두면 경계가 무너진다

### 절 순서는 바꾸지 않는다

```
한 장 요약 → 하지 말 것 → 다시 할 것 → 할 것 → 이 제안이 틀린다면 → 적용 → 부록(근거 상세)
```

`proposal.SECTION_ORDER` 가 원본이다. **"할 것"을 먼저 두면 읽는 사람이 거기서 멈추고
"하지 말 것"은 안 읽힌다.**

- **한 장 요약은 넷을 넘지 않는다** — 발견 · 제안 · 불확실 · 근거(`SUMMARY_KEYS`).
  다섯째 줄이 생기면 잘못 조립한 것이다. 배경·목적은 부록으로 간다
- **「이 제안이 틀린다면」은 문서 전체에 하나다.** 카드마다 두면 아무도 안 읽는다
- **「적용」은 후보가 자동, 고르는 것은 사람이다.** 후보(`판단기준.md` 의 "오늘 내가 내린
  결정" 불릿)는 `candidates` 키로 따로 내보낸다 — `body` 에 넣으면 입력창에 들어가
  사람이 기록을 고칠 수 있게 된다

### 카드에 없는 값을 채워 넣지 않는다

- 자동 절은 **카드에 있는 것만 옮긴다.** 없으면 `"todo"` 로 남긴다.
  채워서 보여주면 그럴듯하지만 **아직 안 정한 것이 정한 것처럼 된다**
- 카드에 `미확인` 이라 적혀 있으면 화면·문서에도 `미확인` 이 그대로 뜬다.
  지금 채우지 않는다 — **미확인은 구멍이 아니라 요청이다**
- 「할 것」 카드가 0장이어도 절을 지우지 않는다. "해당하는 카드가 없습니다"로 남긴다 —
  **비어 있다는 사실이 보여야 한다**
- **화면은 카드를 읽기만 한다** (`parse_cards()` 에 쓰기 경로가 없다). 값을 바꾸려면
  카드 파일로 돌아간다. 화면에서 고치기 시작하면 카드와 화면 중 어느 것이 진짜인지 모르게 된다

### 문장 검사는 새로 만들지 않는다

```python
from report.sections import BANNED, check_phrasing
```

- `sections.py` 의 **같은 함수·같은 목록 객체**를 쓴다. 목록이 둘이면 언젠가 갈라진다
- **자동 절과 사람 절 양쪽에 건다.** 사람이 더 자주 쓴다 (기존 규칙과 같다)
- ⚠ 「이 제안이 틀린다면」 절은 원인을 묻는데 `BANNED` 는 원인 표현을 막는다.
  "표본이 작기 때문에 틀릴 수 있다"(가설)와 "표본이 작기 때문에 낮다"(주장)를
  검사가 구분하지 못한다. **경고일 뿐 차단이 아니므로 오늘은 고치지 않았다** →
  `판단기준.md` 「다음에 확인할 것」

### 내보내기

- **HTML** — `수업자료/제안서_템플릿.html` 의 `<style>` 을 **읽어서 그대로** 쓴다
  (`template_css()`). 스타일을 새로 만들지 않는다. 클래스도 템플릿 것을 쓴다 —
  `num` · `todo` · `unknown` · `prop stop|redo|go` · `badge b-ok|b-warn|b-block|b-none` ·
  `lab l-obs|l-asm|l-est`(관측/가정/추정)
- 외부 CSS·이미지·CDN 을 쓰지 않는다. **단일 파일**이어야 한다
- 관측/가정/추정을 **한 문장으로 합치지 않는다.** 합치면 추정이 실측처럼 읽힌다
- ⚠ 템플릿 본문은 다섯 절(1 발견 · 2 원인 · 3 제안 · 4 틀린다면 · 5 적용)인데
  `build()` 가 만드는 것은 **3·4·5 와 부록**뿐이다. 1·2 는 지어내지 않고 자리만 남긴다.
  **2(원인)는 수요일 몫이고, 금요일 제안서는 이 절이 비면 차단된다**
- **PDF** — `report/to_pdf.py` 의 `Report` 를 그대로 재사용한다. 새 PDF 클래스를 만들지 않는다

### 오늘 실측으로 확인한 것 — 폰트에 없는 글자

`fonts/NotoSansKR-Regular.ttf` 의 cmap 을 직접 열어 확인했다. 위 「문서로 내보낼 때」에
적힌 `−` · `≤` · `②` 말고도 **아래가 더 없다.**

```
①②③  ✕ ▲ ●  「 」  ⚠  ✓  ≥
```

- **`「 」` 가 없다는 것이 중요하다** — 카드 제목과 한 장 요약에 쓰고 있다
- `proposal.PDF_SUBST` 가 바꿔치기하고, `proposal.missing_glyphs()` 가 **바꾼 뒤에도
  남은 글자를 전수로 훑는다.** 남으면 PDF 만들기 전에 화면에 경고가 뜬다
- HTML 은 브라우저 폰트라 그대로 둔다. **같은 내용이라도 HTML 과 PDF 는 표현이 다르다**

### 재현 — PDF 도 같은 입력이면 같은 파일이어야 한다

- ⚠ **fpdf2 는 자기가 `/CreationDate` 에 현재 시각을 박는다.** 내 코드에 `now()` 가
  없어도 파일이 매번 달라진다. `proposal.build_pdf()` 는 카드의 "최종 갱신" 날짜로 고정한다
- ⚠ `report/to_pdf.py` 의 리포트 PDF 는 **아직 재현되지 않는다** — 표지에 `datetime.now()`
  가 있고 `CreationDate` 도 그대로다. 오늘 손대지 않았다

### 자리

- 오늘은 `pages/3_리포트.py` 안 **임시 탭**이다. 독립 메뉴가 아니다.
  기능을 먼저 만들고 자리는 나중에 정한다 — 자리를 먼저 정하면 아직 없는 메뉴에
  맞춰 코드를 짜게 된다
- 세션 저장소를 리포트와 나눈다: `proposal_human` / `human`. 위젯 키도 `ph_`·`pb_` 로 가른다

### 미정

- 「적용」 후보 개수 상한. 지금은 최근 2회차 전부라 19개다 (`RECENT_ENTRIES`)
- 템플릿 1(발견)·2(원인) 절을 `build()` 가 만들지, 다른 곳에서 채울지

---

## 코드를 고칠 때 지킬 것

1. **지표 정의는 `core/config.py` 에서만 바꾼다.**
   단계·임계값·기간·테이블 목록의 유일한 출처는 `config.py` 다.
   `metrics.py` 등에 숫자나 컬럼명을 직접 박지 않는다.
   정의가 바뀌면 도메인 명세(위키)를 먼저 고치고 `config.py` 로 옮긴다.

2. **계산 경로에 현재 시각을 넣지 않는다.**
   `datetime.now()` / `date.today()` / `pd.Timestamp.now()` 를 지표·퍼널·집계 계산에
   쓰지 않는다. 같은 입력이면 언제 돌려도 같은 결과가 나와야 한다 (재현).
   실행 시각은 `core/gates.py` 의 실행 이력(run_id·타임스탬프)에만 남긴다.

3. **시키지 않은 파일은 고치지 않는다.**
   요청에 없는 파일은 건드리지 않는다. 한 번에 한 곳만 바꾼다.
   `viz/`(차트·색), `core/gates.py`, 통계 함수(`_two_prop`·`srm_check`·`status_of`)는
   도메인이 아니므로 지시 없이 바꾸지 않는다 — `DESIGN.md` §4-2.
