# -*- coding: utf-8 -*-
"""앱 설정.

**도메인을 바꿀 때 가장 먼저 고치는 곳이 이 파일이다.**

다만 이 파일만 고치면 끝나지 않는다. 컬럼명은 metrics.py 안에도 박혀 있어서,
config를 다 바꾸고 앱을 켜면 거기서 깨진다. **깨지는 것이 정상이다.**
순서는 DESIGN.md §4-6에 있다.

    1. 여기 FUNNEL_STEPS 를 내 단계로 바꾼다
    2. load.py 가 내 파일을 읽게 한다
    3. 앱을 켠다 -> 깨진다
    4. metrics.py 의 컬럼명을 하나씩 맞춘다
    5. validate.py 에서 내 데이터에 맞지 않는 검증을 뺀다

────────────────────────────────────────────────────────────────────
7주차 토요일에 만든 「명세 5행」이 여기로 들어온다.

  명세 1행  데이터                 ->  DATASET · PERIOD · TABLES
  명세 2행  퍼널 단계              ->  FUNNEL_STEPS · FUNNEL_LABELS
  명세 3행  주지표 + 가드레일       ->  metrics.py 에서 계산
  명세 4행  임계값 · 최소 표본      ->  THRESHOLDS · MIN_SAMPLE
  명세 5행  유지 / 이탈의 정의      ->  RETENTION_STEPS
────────────────────────────────────────────────────────────────────

**명세 5행은 mydomain/나의_성장퍼널.md 상단 표가 원본이다.** 이 파일은 그것을 옮긴 것일 뿐,
여기서 새로 정하지 않는다. 어긋나면 config 가 아니라 명세가 맞다.
각 값의 실측 근거는 mydomain/notes/ 에 있다 (01_데이터 · 02_퍼널실측 · 03_지표정의 · 04_유지이탈).

아직 비어 있는 것 둘 — 둘 다 **일부러** 비워 두었고 사유는 해당 위치에 적혀 있다:
  RETENTION_STEPS   명세 5행은 확정됐으나 합성 데이터에 Notifier 컬럼이 없어 계산 불가
  가드레일 임계값     정의서가 "사용자가 정할 것"으로 남겨둔 칸
"""
from pathlib import Path

ROOT = Path(__file__).resolve().parent.parent
DATA_DIR = ROOT / "data"          # 데이터 파일
RUNS_DIR = ROOT / "runs"          # 실행 이력
ASSETS_DIR = ROOT / "assets"      # 폰트 등

# ── 데이터 ────────────────────────────────────────────────────────
# ★ Day1 — 명세 1행
#
#   도메인: FDA GRAS 등록 심사가 어느 단계에서 얼마나 걸러지는지 보고,
#          등록을 준비하는 실무 조직에게 제출 전 점검 우선순위를 제안한다.
#
#   ⚠ 앱이 읽는 테이블 3개 중 **실제는 GRASNotices 하나**다.
#     결론의 근거는 그것만 쓰고, cases/funnel_events_synthetic 은 연습용이다.
#     이 이름이 사이드바·PDF 머리말·실행 이력에 그대로 찍힌다.
DATASET = "FDA_GRAS_등록심사"
#   명세 1행의 기간을 그대로 쓴다 — 실제 데이터의 유효구간(종결일 기준)이고
#   지표 정의서가 선언한 구간이다.
#
#   ⚠ 합성 이벤트는 2025-09-01 ~ 2027-01-14 이라 뒤쪽이 이 구간을 넘어간다.
#     버그가 아니라 **미래로 시뮬레이션한 합성 데이터**라서 그렇다.
#     Day1 검증(날짜 범위)에서 이것이 잡히면 block 이 아니라 **warn** 이다 —
#     이 값으로 계산이 틀리는 것이 아니라 해석할 때 구분하면 되는 것이다.
PERIOD = ("1998-05-27", "2026-07-06")

# ★ 내 테이블 목록. 하나뿐이어도 된다. 파일명(확장자 제외)과 같아야 한다.
TABLES = [
    "GRASNotices",              # **실제** FDA GRAS 통보 1,336건 — 결론의 근거는 이것만 쓴다
    "cases_synthetic",          # 합성 2,000건 · case_id · host_identified · country
    "funnel_events_synthetic",  # 합성 이벤트 6,064건 · case_id · stage · event_time
]
# ⚠ GRASNotices 는 **parquet 을 읽는다**(load.READERS 가 .parquet 을 먼저 찾는다).
#   원본 data/GRASNotices.csv 는 앱이 못 읽는다 — cp1252 인코딩 · 머리말 2줄 ·
#   =T("...") 엑셀 수식 껍데기 때문이다. 원본은 손대지 않고 그대로 둔다.
#
#   정제는 mydomain/measure.py 의 load() 를 **그대로 재사용**해서 했다.
#   규칙을 새로 짜면 mydomain/notes/ 의 숫자와 어긋나기 때문이다.
#   재현 확인: 1,336행 · 종결 1,301 · 이의없이종결 1,056 · 체인 1,259/1,224/1,055
#             · 종결→결과 81.17% · 체인 최종성공률 86.19% — 전부 노트와 일치.
#
#   파생 컬럼 (원본 29열 + 12열):
#     grn          GRN 번호 (=T("15") 껍데기를 벗긴 정수)
#     closure_date 종결일 · filing_date 접수일 (2017년 이후 없음 — 제도상 비공개)
#     cat          FDA's Letter 14종 표기 -> no_questions · ceased · no_basis · Pending · MIXED
#     bio          축① 숙주식별 / 미분류      country  축② 미국 / 해외 / 미상
#     chain        재제출 체인 id (union-find)  resub_of · resub_by  재제출 링크
#     host · ctry · close_yr

# ── 퍼널 ──────────────────────────────────────────────────────────
# ★ Day1 — 명세 2행. 순서가 곧 단계 순서다.
#
#   **6단계가 아니라 3단계다.** GRAS 심사는 상태 전이가 얕아 6단계가 없다.
#   교안 4-4: 단계 수는 도메인이 정한다.
#
#   각 단계를 실제 데이터에서 무엇으로 세는가 (mydomain/notes/퍼널실측_2026-08-29.md §0):
#
#       제출   GRN 1건이 인벤토리에 존재
#       종결   FDA's Letter != Pending  (= Date of closure 있음, 완전 일치 확인함)
#       결과   FDA's Letter == "no questions"  (이의 없이 종결)
#
#   진짜 퍼널인가? — **그렇다.** 순서 위반 0건으로 확인했다.
#   결과 ⊆ 종결 ⊆ 제출 이 모든 구간에서 성립한다. 분류가 아니라 순서가 강제되는 퍼널이다.
#
#   병목은 **종결 → 결과** 다 (합성 80.09% · 실제 81.17%).
#   제출 → 종결에서 빠지는 건은 탈락이 아니라 **아직 진행 중**인 것이다.
FUNNEL_STEPS = ["제출", "종결", "결과_성공"]
FUNNEL_LABELS = {
    "제출": "제출",
    "종결": "종결",
    "결과_성공": "결과(성공)",
}
# ⚠ "결과_실패"(387건)는 **단계가 아니라 결과 분기**다. 그래서 위에 넣지 않았다.
#   넣으면 종결 뒤에 두 갈래가 생겨 "앞 단계를 반드시 거치는가"가 깨진다.
#
# ⚠ Day2 에서 metrics.funnel() 을 채울 때 — **행이 아니라 고유 case_id 로 센다.**
#   funnel_events_synthetic.csv 6,064행에 완전중복 176행이 일부러 섞여 있어,
#   행으로 세면 제출이 2,072건, nunique 로 세면 2,000건이 된다.

# ── 유지 퍼널 ─────────────────────────────────────────────────────
# ★ Day2 — 명세 5행. 7주차 토요일에 내가 정한 유지·이탈의 정의를 옮긴다.
#
#   획득 퍼널은 단계가 주어졌지만 유지는 순서가 없다. 내가 정의해야 한다.
#   각 단계는 (이름, 설명) 이고, 계산은 metrics.retention_funnel() 에서 한다.
#
#   ── 명세 5행은 **확정되어 있다.** 다만 지금 데이터로는 계산할 수 없다. ──
#
#   확정 정의 (mydomain/notes/04_유지이탈.md):
#
#       이탈 = 첫 제출 후 1,095일(3년) 안에 재방문이 없음
#       단위 = Notifier(회사).  물질(GRN)에는 재방문 개념이 없지만
#              **같은 회사가 다른 물질로 다시 제출하는가**는 실재하는 신호다.
#       실측 = 코호트 N=688 · 잔존 138(20.06%) · 이탈 550(**79.94%**)
#
#   창 길이별 재방문율: 1년 11.74% · 2년 17.65% · **3년 20.06%** · 5년 23.38%
#   3년은 정답이 있어서가 아니라 넷 중 사용자가 고른 대표값이다.
#
#   ⚠ **왜 비워 두었는가 — Notifier(회사) 컬럼이 없다.**
#     앱이 읽는 합성 데이터는 case_id · host_identified · country 3개 컬럼뿐이라
#     "누가 다시 왔는가"를 물을 대상 자체가 없다. 회사 정보는 실제 GRASNotices 에만 있다.
#     억지로 case 단위로 바꾸면 원칙①("다시 왔는가")의 답이 아닌 다른 것을 재게 된다.
#
#   → **이것은 Day4 리포트 7장(한계)로 올라가야 한다.** "찾았는데 없던 것도 결과다."
#     report/sections.py _s7_limits() 참조.
#
#   ⚠ 계산할 때 지킬 것 — 관측 기간이 다른 대상을 그냥 비교하지 않는다.
#     코호트(창을 다 채울 만큼 오래된 회사)로 거르지 않고 "2건 이상 낸 회사 비율"을 쓰면
#     26.53% 가 나와 3년 창 값(20.06%)보다 **오히려 높다.** 최근에 처음 제출한 회사가
#     재방문할 시간이 없었을 뿐인데 전부 이탈로 잡히기 때문이다.
RETENTION_STEPS: list[tuple[str, str]] = []

# ── 비즈니스 파라미터 ─────────────────────────────────────────────
# 전부 **가정값**이다. 실측 테이블에서 유도하지 않는다.
# 광고비(ad_spend)는 채널 효율(ROAS·CPC) 전용이고, 총액을 가입자 수로 나눠
# CAC를 만들지 않는다 — 광고비는 개인 단위로 추적되지 않아 가입과 이을 수 없다.
# 리포트에 이 값이 들어가는 문장에는 "가정값 기반"을 붙인다. -> DESIGN.md §4-3
#
# ★ 비용 개념이 없는 도메인이면 투입 공수(인시)로 바꿔도 된다. 금액이 아니어도
#   "무엇을 얼마나 썼는가"면 된다.
#
#   **이 도메인에는 셋 다 없다.** GRAS 심사에는 획득 경로(채널)도, 광고비도,
#   공헌이익도, 평균 유지기간도 존재하지 않는다. 제출자는 FDA 심사 절차를 못 바꾸고
#   데이터에 비용 컬럼이 아예 없다.
#
#   통신사 가정값(검색 140,000원 등)을 그대로 두면 남의 도메인 숫자가 리포트로
#   새어 들어가므로 **비운다.** 이 셋을 읽는 곳은 metrics.channel_efficiency()
#   하나뿐이고, 그것은 Day3 **선택 과제**라 비어 있어도 나머지가 돈다.
CHANNEL_CAC: dict[str, int] = {}
CONTRIBUTION_MARGIN = None      # 공헌이익률 — 해당 없음
LTV_MONTHS = None               # 평균 유지기간 — 해당 없음

# ── 지표 임계값 ───────────────────────────────────────────────────
# ★ Day1 — 명세 4행. **비워 두었다. 내가 정한다.**
#
#   남이 정해줄 수 없는 유일한 값이다. 업계 벤치마크·직전 기간·목표치 중
#   무엇을 근거로 삼았는지 위키 지표 정의서에 한 줄로 적는다.
#   근거 없는 임계값은 남의 것이다.
#
#   지금은 비어 있어서 지표 카드가 전부 "정상"으로 뜬다. 채우면 색이 갈린다.
#
#   형식:  "지표이름": {"경고": 값, "위험": 값}
#
#   근거 (mydomain/notes/03_지표정의.md) — 주지표는 **최종 성공률**이다:
#     경고 80%  95% CI 하단(84.15%)보다 낮게 잡아, 정상 변동이 아니라
#               확실히 나빠졌을 때만 울리게 했다
#     위험 75%  현재값(86.19%) 대비 약 11%p 하락한 보수적인 선
#
#   ⚠ 키 이름은 Day2 에 metrics.kpis() 가 돌려줄 지표 이름과 **똑같아야** 한다.
#     이름이 어긋나면 status_of() 가 임계값을 못 찾아 전부 "정상"으로 뜬다.
#
#   ⚠ 합성 데이터의 종결→결과가 80.09% 라 경고선(80%)에 거의 붙어 있다.
#     이 임계값은 실제 체인 기준 주지표(86.19%)를 놓고 정한 것이므로 정상이지만,
#     합성 데이터로는 화면에 바로 경고색이 뜰 수 있다.
THRESHOLDS: dict[str, dict[str, float]] = {
    "최종 성공률": {"경고": 80.0, "위험": 75.0},
}

# ── 가드레일 (명세 3행) — 임계값 미정 ─────────────────────────────
# 주지표(성공률)를 올리려 할 때 **실제로 희생될 수 있는 것** 둘이다.
# 후보 4개(리드타임·재제출비율·자진철회율·Notifier이탈률) 중 방향성 근거가
# 가장 분명한 둘을 채택했다.
#
#   ① 종결까지 소요 기간 (리드타임)
#      실제  N=678 · 평균 195.0일 · 중앙 176일 · P90 290일
#            ⚠ 접수일이 있는 1998~2019 구간에서만 계산된다 (2017년 이후 비공개)
#      합성  N=1,944 · 평균 113.8일 · 중앙 113일 · P90 183일
#
#   ② 재제출 필요 비율
#      실제  5.80% (다건 체인 73 / 전체 체인 1,259)
#      합성  **계산 불가** — 합성 데이터에는 재제출 체인 개념이 없다
#
# **임계값(경고·위험)은 아직 정하지 않았다.** 정의서가 "사용자가 정할 것"으로
# 비워둔 칸이고, 근거 없는 임계값은 남의 것이므로 지어내지 않는다.
#
# 정할 때 함께 할 일 — 둘 다 **높을수록 나쁜** 지표다.
# metrics.status_of() 의 higher_is_worse 집합에 같은 이름을 넣어야 색이 뒤집히지 않는다.

# ★ 이보다 표본이 적으면 "표본 부족"으로 보고 지표를 계산하지 않는다.
#   내 상황에 맞게 낮춰도 된다. **다만 없애지는 않는다.** -> DESIGN.md §4-5
#
#   30 은 교안 기본값을 그대로 썼다. ⚠ 월평균 표본이 3.94건이라 311개월 중
#   이 기준을 채우는 달이 **0개**다 — 이 임계값은 **분기·연도 단위 집계에서만**
#   실질적으로 적용된다. 월별 수치는 참고용이지 판단 근거로 쓸 수 없다.
MIN_SAMPLE = 30

# ── 상태 색 ───────────────────────────────────────────────────────
# 의미가 고정된 규칙이다. **도메인이 바뀌어도 바꾸지 않는다.**
COLORS = {
    "ok": "#10b981",       # 통과 / 정상
    "warn": "#f59e0b",     # 경고 / 표본 부족 / 가드레일 악화
    "block": "#f43f5e",    # 차단 / 검증 실패 / 실험 무효
    "none": "#64748b",     # 데이터 없음 / 효과 없음
}
# 브랜드 색은 자유롭게 바꿔도 된다.
BRAND = {
    "primary": "#4f46e5",
    "ink": "#0f172a",
    "muted": "#64748b",
    "line": "#e2e8f0",
    "bg": "#f8fafc",
    "surface": "#ffffff",
}

# ── 리포트 ────────────────────────────────────────────────────────
# 8장 중 3장은 사람이 쓴다. **이 구분은 바꾸지 않는다.**
# 해석과 제안을 자동화하는 순간 책임이 사라진다.
REPORT_SECTIONS = [
    ("1. 요약", "auto"),
    ("2. 배경", "human"),
    ("3. 방법", "auto"),
    ("4. 결과", "auto"),
    ("5. 실험", "auto"),
    ("6. 해석", "human"),
    ("7. 한계", "auto"),
    ("8. 제안", "human"),
]

# 발송은 초안까지만. 실제로 전송하지 않는다.
EMAIL_ENABLED = False
EMAIL_TO_EXAMPLE = "team-growth@example.com"
